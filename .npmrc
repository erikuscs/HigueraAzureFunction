# Force latest versions of packages with known issues
inflight@2.0.0
glob@10.4.5
rimraf@5.0.10
# Removed deprecated lodash.isequal

# Override vulnerable packages
fstream@>=2.0.0  # Note: This package is no longer supported and should be replaced

# Dependency resolution strategies
legacy-peer-deps=false
fund=false

# Added comments about deprecations
# Note: lodash.isequal is deprecated - consider using require('node:util').isDeepStrictEqual instead
# Note: rimraf versions prior to v4 are supported; current specification is 5.0.10